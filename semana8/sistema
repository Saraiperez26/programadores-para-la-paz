El comando uname -a muestra información detallada del sistema operativo, como el nombre del sistema, versión del kernel y arquitectura del equipo.

El comando whoami muestra el nombre del usuario que está utilizando actualmente el sistema.

Conocer esta información es útil al administrar un servidor porque permite identificar el entorno de trabajo, verificar configuraciones y solucionar problemas de manera más eficiente.
